vue：
生命周期（85%）；在哪个钩子请求为什么？；
双向绑定原理；（看你答得程度有可能会延伸比如是在什么时后监听）（60%）；
常见的指令；（比如v-if,v-for他俩的优先级，key的作用）（60%大概说说）；
如何封装一个组件；(70%)；
watch和computer的区别watch和computer原理(85%)；
组件传值方式，自定义组件如何传值（95%）；
vuex；（几个属性及区别及怎么调用，在项目中如何使用）（90%）；
插槽（30%）；
axios:请求拦截，响应拦截，（85%）；
登录拦截（流程）（80%）；
有几种导航守卫（50%）；
history和hash模式（区别）默认是哪种（70%）；
$nexttick原理（60%）；
keepalive原理；
项目优化（70%）；
element 如何给动态添加校验rules；

js：
防抖节流，区别，实现方式，使用场景（95%）；
改变this指向方式区别 可能会现场出题反问你（85%）；
事件循环机制evenloop(90%);
判断数组的方法；instanceof原理是什么如何实现？（60%）；
数组排重几种方式,可能是js答题卡（80%）；
split和spice的区别，for in ,for of 区别；
map会改变原数组吗？和fifter的区别；（80%）；
js获取两个字符串相同的部分；（60%）；
js判断一个字符串出现最多的次数的字符并统计次数（60%）；
JS中计算出现浮点数计算精度问题怎么解决（80%）；
js计算微任务宏任务执行顺序就是给你一道面试题让你写出执行结果及顺序（90%）；
闭包，优缺点，闭包变量存储在哪里堆还是栈 ，可能会出个使用场景反向问你其实就是看你有没有用过闭包，（90%）；
箭头函数this指向是否可改变为什么（90%）；
深拷贝浅拷贝；原型原型链跟你你的回答会深入拓展问（90%）；
new实现（50%）；
cookie sessionstorage localstorage区别（85%）延申会问token怎么存储；
缓存（强缓存、协商缓存）（95%）；

es6：
var const let：（95%）
1、区别
2、const const定义的值能改么?
	部分能，部分不能;const定义的基本类型不能改变，但是定义的对象是可以通过修改对象属性等方法来改变的;
3、let的暂存性死区；
4、let const 会变量提升吗?（不会）

promise:（100%）
并行执行和顺序执行；
能手写一个promise,promise几个状态，promise链式调用，链式调用中怎么终止.then（throw抛出）；
ansy awite （优缺点）；
有这个面试思考题：
问：当发送一个请求后；后端不能第一时间给出终止请求的判断条件（可能是一个bool值）；前端需要隔几秒调用一次直到得到这个字段为true请求
那么请问如何使用ansy awite优雅的解决；（看不明白可以直接pass这个。。。）；
解构赋值，字符串拼接之类的（50%）；
es6和es5区别（90%）；

css:
响应式布局；适配方案；flex;
flex:0 1 auo是什么意思；
跳转详情也返回的时候还停留在之前浏览位置要怎么实现；（大概是通过浏览器计算存储之前的位置）
优化：
图片懒加载；（可能让你手写一个图片懒加载呵呵）
首页加载优化；

延申：
前端工程化理解；
浏览器缓存机制；
是否配置过webpack举例说明（loader和plugin）；
webgl了解吗；
ts;
单元测试；
重绘和重排?
findIndex filter map的区别
引用类型去重
二分查找
怎么用js实现多线程
async awite大致原理
翻转字符串
promise实现图片懒加载(不重要)
事件流----
cookie方法
管理系统全选怎么做
长列表怎么优化
各个角色的权限 页面路由权限 按钮权限
scope的原理
关于http的架构了解多少 封装过么
设计模式了解多少
单页面对seo不友好 怎么办 ssr可以解决 怎么实现的
然后http里面常用的字段 几种鉴权方式

移动端适配  cookie方法 权限控制 
浏览器缓存原理，其实涉及到weapack如何实现缓存。浏览器第一次加载为什么这么慢后来就快了
很基础的 css那些样式可以继承 实现一个左右布局有那些方式 
Object.defindproperty与proxy的区别 
react的生命周期 hooks